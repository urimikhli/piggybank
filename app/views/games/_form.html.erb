<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_name %><br />
    <%= f.text_field :game_name %>
  </div>
  
  <div class="field">
    <%= f.label :studio_name %><br />
    <%= f.collection_select(:studio_id, @studios, :id, :name, :include_blank => "Please select")%>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
